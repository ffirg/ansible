all:
  hosts:
    foobar1.host:
      interfaces:
        frontend: foobar1-fe.host
        backend: foobar1-be.host
        management: foobar1-mgmt.host
      ansible_host: "{{ interfaces[target_network | default('management')] }}"
    foobar2.host:
      interfaces:
        frontend: foobar2-fe.host
        backend: foobar2-be.host
        management: foobar2-mgmt.host
      ansible_host: "{{ interfaces[target_network | default('management')] }}"
  vars:
    ansible_connection: local
    ansible_python_interpreter: '{{ ansible_playbook_python }}'
