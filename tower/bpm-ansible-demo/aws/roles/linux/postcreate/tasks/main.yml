---
# This playbook contains post VM creation tasks that will be run on all Linux nodes.

#- name: Create user accounts
#  user: name=phil comment="Phil Griffiths" uid=1101 groups=wheel append=yes

#- name: Install latest NTP package
#  yum: name=ntp state=latest

#- name: Configure NTP service
#  template: src=ntp.conf.j2 dest=/etc/ntp.conf owner=root mode=0644
#  notify:
#    - restart ntp

#- name: Ensure NTP is started at boot
#  service: name=ntpd state=started enabled=yes

- name: Send out login details via email
  mail:
    host='email-smtp.eu-west-1.amazonaws.com'
    port=587
    username=AKIAJP43DRPPY5HSIQIQ
    password='AmueIT9s4roHW8mN/JavXpEyJzxT+ZMPePs74Jyf4zxu'
    from="noreply@bangonabout.com (Ansible VM Creator)"
    headers=Reply-To=helpdesk@mailinator.com
    to="requester <demo@mailinator.com>"
    subject='Your VM has been created'
    body='Your "{{ ec2_os_type }}" instance has been created - "{{ hostvars[justcreatedlinux][ec2_dns_name] }}"'
