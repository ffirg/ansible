---
- name: RHV Discovery
  hosts: all

  tasks:

  - name: Authenticate against RHVM
    ovirt_auth:
      url: '{{ OVIRT_URL }}'
      username: '{{ OVIRT_USERNAME }}'
      password: '{{ OVIRT_PASSWORD }}'

  - name: List VMs
    ovirt_vms_facts:
